<h1><%= @memo.title %></h1>

<p><%= @memo.body %></p>

<ul>
  <li><%= link_to "Edit", edit_memo_path(@memo) %></li>
  <li><%= link_to "Destroy", memo_path(@memo), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you Sure?"
                  } %></li>
</ul>
